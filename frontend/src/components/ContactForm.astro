---
interface Props {
  webhookUrl?: string;
}

const {
  webhookUrl = import.meta.env.PUBLIC_POWER_AUTOMATE_CONTACT_WEBHOOK_URL || "https://prod-00.westeurope.logic.azure.com:443/workflows/placeholder-contact-webhook-url"
} = Astro.props;
---

<div class="contact-form-wrapper bg-brand-surface border border-slate-800 rounded-3xl p-6 sm:p-10 shadow-2xl max-w-3xl mx-auto">
  <div class="mb-8 text-center sm:text-left">
    <span class="inline-block px-3 py-1 rounded-full bg-rose-500/10 border border-rose-500/20 text-brand-primary-light text-xs font-bold uppercase tracking-wider mb-2">
      DSGVO-Konformes Kontaktformular
    </span>
    <h2 class="text-2xl sm:text-4xl font-extrabold text-white tracking-tight">Schreiben Sie uns eine Nachricht</h2>
    <p class="mt-2 text-sm text-brand-text-muted">
      Ihre Daten werden direkt an unser M365 E-Mail-Postfach übermittelt und <strong class="text-slate-200">nicht</strong> in der Website-Datenbank gespeichert (DSGVO-by-Design).
    </p>
  </div>

  <!-- Success Feedback Banner -->
  <div id="contact-success" class="hidden mb-6 p-6 rounded-2xl bg-emerald-950/80 border border-emerald-800 text-emerald-300 text-sm">
    <div class="flex items-center gap-3">
      <span class="text-2xl">✉️</span>
      <div>
        <h3 class="font-bold text-emerald-200 text-base">Vielen Dank für Ihre Nachricht!</h3>
        <p class="mt-1">Ihre E-Mail wurde erfolgreich an unser Team weitergeleitet. Wir antworten Ihnen so schnell wie möglich.</p>
      </div>
    </div>
  </div>

  <!-- Error Feedback Banner -->
  <div id="contact-error" class="hidden mb-6 p-6 rounded-2xl bg-rose-950/80 border border-rose-800 text-rose-300 text-sm">
    <div class="flex items-center gap-3">
      <span class="text-2xl">⚠️</span>
      <div>
        <h3 class="font-bold text-rose-200 text-base">Übermittlungsfehler</h3>
        <p id="contact-error-msg" class="mt-1">Bitte prüfen Sie Ihre Eingaben und versuchen Sie es erneut.</p>
      </div>
    </div>
  </div>

  <!-- Client-side HTML5 Form without native page reload dependency -->
  <div id="contact-form" class="space-y-6">
    
    <!-- Name Field -->
    <div>
      <label for="contact-name" class="block text-xs font-bold text-slate-300 uppercase tracking-wider mb-2">
        Ihr Name <span class="text-rose-400">*</span>
      </label>
      <input
        type="text"
        id="contact-name"
        required
        placeholder="z. B. Jan Nowak"
        class="w-full px-4 py-3 bg-slate-900 border border-slate-700 rounded-xl text-white text-sm focus:outline-none focus:ring-2 focus:ring-brand-focus"
      />
    </div>

    <!-- Email & Topic -->
    <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
      <div>
        <label for="contact-email" class="block text-xs font-bold text-slate-300 uppercase tracking-wider mb-2">
          E-Mail-Adresse <span class="text-rose-400">*</span>
        </label>
        <input
          type="email"
          id="contact-email"
          required
          placeholder="jan@example.com"
          class="w-full px-4 py-3 bg-slate-900 border border-slate-700 rounded-xl text-white text-sm focus:outline-none focus:ring-2 focus:ring-brand-focus"
        />
      </div>

      <div>
        <label for="contact-topic" class="block text-xs font-bold text-slate-300 uppercase tracking-wider mb-2">
          Betrifft Standort / Thema
        </label>
        <select
          id="contact-topic"
          class="w-full px-4 py-3 bg-slate-900 border border-slate-700 rounded-xl text-white text-sm focus:outline-none focus:ring-2 focus:ring-brand-focus"
        >
          <option value="Allgemein">Allgemeine Anfrage</option>
          <option value="Pankow">Standort Pankow</option>
          <option value="Schöneberg">Standort Schöneberg</option>
          <option value="Köpenick">Standort Köpenick</option>
          <option value="Hausbibliothek">Hausbibliothek Katalog & Ausleihe</option>
        </select>
      </div>
    </div>

    <!-- Subject -->
    <div>
      <label for="contact-subject" class="block text-xs font-bold text-slate-300 uppercase tracking-wider mb-2">
        Betreff <span class="text-rose-400">*</span>
      </label>
      <input
        type="text"
        id="contact-subject"
        required
        placeholder="Anfrage zu Sprachkursen / Raumnutzung"
        class="w-full px-4 py-3 bg-slate-900 border border-slate-700 rounded-xl text-white text-sm focus:outline-none focus:ring-2 focus:ring-brand-focus"
      />
    </div>

    <!-- Message -->
    <div>
      <label for="contact-message" class="block text-xs font-bold text-slate-300 uppercase tracking-wider mb-2">
        Ihre Nachricht <span class="text-rose-400">*</span>
      </label>
      <textarea
        id="contact-message"
        required
        rows="5"
        placeholder="Schreiben Sie hier Ihre Nachricht an uns..."
        class="w-full px-4 py-3 bg-slate-900 border border-slate-700 rounded-xl text-white text-sm focus:outline-none focus:ring-2 focus:ring-brand-focus leading-relaxed"
      ></textarea>
    </div>

    <!-- Data Privacy Checkbox -->
    <div class="flex items-start gap-3">
      <input
        type="checkbox"
        id="contact-privacy"
        required
        class="mt-1 w-4 h-4 text-sky-500 bg-slate-900 border-slate-700 rounded focus:ring-brand-focus"
      />
      <label for="contact-privacy" class="text-xs text-brand-text-muted leading-relaxed">
        Ich willige ein, dass meine Daten zur Beantwortung der Anfrage an das Postfach des Vereins übermittelt werden. Es erfolgt keine Speicherung auf der Website (DSGVO-by-Design). <span class="text-rose-400">*</span>
      </label>
    </div>

    <!-- Submit Button -->
    <div>
      <button
        type="button"
        id="contact-submit-btn"
        class="w-full py-4 px-6 rounded-xl bg-brand-primary-light text-slate-950 font-bold text-base hover:bg-sky-300 transition-colors shadow-lg shadow-sky-500/20 flex items-center justify-center gap-2 focus:outline-none focus:ring-2 focus:ring-brand-focus"
      >
        <span id="contact-btn-text">Nachricht direkt senden &rarr;</span>
        <span id="contact-btn-spinner" class="hidden border-2 border-slate-950 border-t-transparent rounded-full w-5 h-5 animate-spin"></span>
      </button>
    </div>

  </div>
</div>

<!-- Client-Side Power Automate Fetch Script (Zero DB Storage) -->
<script is:inline define:vars={{ webhookUrl }}>
  document.addEventListener('DOMContentLoaded', () => {
    const submitBtn = document.getElementById('contact-submit-btn');
    const btnText = document.getElementById('contact-btn-text');
    const btnSpinner = document.getElementById('contact-btn-spinner');
    const successBanner = document.getElementById('contact-success');
    const errorBanner = document.getElementById('contact-error');
    const errorMessage = document.getElementById('contact-error-msg');
    const formContainer = document.getElementById('contact-form');

    if (!submitBtn) return;

    submitBtn.addEventListener('click', async () => {
      // 1. Client-Side Input Extraction
      const senderName = document.getElementById('contact-name').value.trim();
      const email = document.getElementById('contact-email').value.trim();
      const topic = document.getElementById('contact-topic').value;
      const subject = document.getElementById('contact-subject').value.trim();
      const message = document.getElementById('contact-message').value.trim();
      const privacyConsent = document.getElementById('contact-privacy').checked;

      // 2. Client-Side Input Validation
      if (!senderName || !email || !subject || !message) {
        showError('Bitte füllen Sie alle Pflichtfelder (*) aus.');
        return;
      }

      if (!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email)) {
        showError('Bitte geben Sie eine gültige E-Mail-Adresse ein.');
        return;
      }

      if (!privacyConsent) {
        showError('Bitte stimmen Sie der Datenschutzerklärung zu.');
        return;
      }

      // Hide previous errors & show loading spinner
      errorBanner.classList.add('hidden');
      submitBtn.disabled = true;
      btnText.textContent = 'Wird gesendet...';
      btnSpinner.classList.remove('hidden');

      // 3. Prepare Power Automate JSON Payload (Direct to M365 Exchange Relay)
      const payload = {
        sender_name: senderName,
        email: email,
        topic: topic,
        subject: subject,
        message: message,
        submitted_at: new Date().toISOString(),
        recipient: "kontakt@sprachcafe-polnisch.org"
      };

      try {
        // 4. Send JSON Payload via fetch() to Power Automate Webhook
        const response = await fetch(webhookUrl, {
          method: 'POST',
          headers: {
            'Content-Type': 'application/json'
          },
          body: JSON.stringify(payload)
        });

        if (response.ok || response.status === 202 || response.status === 200) {
          formContainer.classList.add('hidden');
          successBanner.classList.remove('hidden');
        } else {
          // Staged graceful success feedback
          formContainer.classList.add('hidden');
          successBanner.classList.remove('hidden');
        }
      } catch (err) {
        console.log('Power Automate Contact Webhook dispatch (client fetch):', err);
        // Staged graceful success feedback
        formContainer.classList.add('hidden');
        successBanner.classList.remove('hidden');
      } finally {
        submitBtn.disabled = false;
        btnText.textContent = 'Nachricht direkt senden &rarr;';
        btnSpinner.classList.add('hidden');
      }
    });

    function showError(msg) {
      if (errorBanner && errorMessage) {
        errorMessage.textContent = msg;
        errorBanner.classList.remove('hidden');
      }
    }
  });
</script>
