name: Continuous Deployment Pipeline

on:
  push:
    branches: [ main, beta ]
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  # =========================================================================
  # JOB 1: Deploy to Staging Environment (beta.sprachcafe-polnisch.org)
  # =========================================================================
  deploy-staging:
    name: Deploy to Staging (beta.sprachcafe-polnisch.org)
    if: github.ref == 'refs/heads/beta' || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🟢 Setup Node.js Environment
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: 📦 Install Frontend Dependencies
        run: |
          cd frontend
          npm ci

      - name: 🏗️ Build Astro Site & Generate Pagefind Index
        run: |
          cd frontend
          npm run build

      - name: 🔑 Configure SSH Key & Known Hosts
        env:
          SSH_PRIVATE_KEY: ${{ secrets.LIGHTSAIL_SSH_KEY }}
          SSH_HOST: ${{ secrets.LIGHTSAIL_HOST }}
        run: |
          if [ -n "$SSH_PRIVATE_KEY" ] && [ -n "$SSH_HOST" ]; then
            mkdir -p ~/.ssh
            echo "$SSH_PRIVATE_KEY" > ~/.ssh/id_rsa
            chmod 600 ~/.ssh/id_rsa
            ssh-keyscan -H "$SSH_HOST" >> ~/.ssh/known_hosts 2>/dev/null || true
          else
            echo "⚠️ LIGHTSAIL_SSH_KEY or LIGHTSAIL_HOST not set. Skipping SSH setup for dry-run validation."
          fi

      - name: 🚀 Deploy /dist via rsync to Staging (/var/www/beta)
        env:
          SSH_KEY: ${{ secrets.LIGHTSAIL_SSH_KEY }}
          SSH_HOST: ${{ secrets.LIGHTSAIL_HOST }}
          SSH_USER: ${{ secrets.LIGHTSAIL_USER || 'deploy' }}
          TARGET_DIR: /var/www/beta/
        run: |
          if [ -n "$SSH_KEY" ] && [ -n "$SSH_HOST" ]; then
            rsync -avz --delete \
              -e "ssh -i ~/.ssh/id_rsa -o StrictHostKeyChecking=no" \
              frontend/dist/ \
              ${SSH_USER}@${SSH_HOST}:${TARGET_DIR} || \
            rsync -avz --delete \
              -e "ssh -i ~/.ssh/id_rsa -o StrictHostKeyChecking=no" \
              frontend/dist/ \
              ubuntu@${SSH_HOST}:${TARGET_DIR}
            
            # Reload Caddy Server
            ssh -i ~/.ssh/id_rsa -o StrictHostKeyChecking=no ${SSH_USER}@${SSH_HOST} \
              "docker exec sprachcafe_caddy caddy reload --config /etc/caddy/Caddyfile 2>/dev/null || docker exec caddy caddy reload --config /etc/caddy/Caddyfile 2>/dev/null || docker exec library_proxy caddy reload --config /etc/caddy/Caddyfile 2>/dev/null || true" 2>/dev/null || true
            
            echo "✅ Successfully deployed to Staging (${TARGET_DIR})"
          else
            echo "⚠️ Deployment skipped: LIGHTSAIL_SSH_KEY secret not present."
          fi

  # =========================================================================
  # JOB 2: Deploy to Production Environment (sprachcafe-polnisch.org & sprachcafé.org)
  # =========================================================================
  deploy-production:
    name: Deploy to Production (sprachcafe-polnisch.org & sprachcafé.org)
    if: github.ref == 'refs/heads/main' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🟢 Setup Node.js Environment
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: 📦 Install Frontend Dependencies
        run: |
          cd frontend
          npm ci

      - name: 🏗️ Build Astro Site & Generate Pagefind Index
        run: |
          cd frontend
          npm run build

      - name: 🔑 Configure SSH Key & Known Hosts
        env:
          SSH_PRIVATE_KEY: ${{ secrets.LIGHTSAIL_SSH_KEY }}
          SSH_HOST: ${{ secrets.LIGHTSAIL_HOST }}
        run: |
          if [ -n "$SSH_PRIVATE_KEY" ] && [ -n "$SSH_HOST" ]; then
            mkdir -p ~/.ssh
            echo "$SSH_PRIVATE_KEY" > ~/.ssh/id_rsa
            chmod 600 ~/.ssh/id_rsa
            ssh-keyscan -H "$SSH_HOST" >> ~/.ssh/known_hosts 2>/dev/null || true
          else
            echo "⚠️ LIGHTSAIL_SSH_KEY or LIGHTSAIL_HOST not set. Skipping SSH setup for dry-run validation."
          fi

      - name: 🚀 Deploy /dist via rsync to Production (/var/www/production)
        env:
          SSH_KEY: ${{ secrets.LIGHTSAIL_SSH_KEY }}
          SSH_HOST: ${{ secrets.LIGHTSAIL_HOST }}
          SSH_USER: ${{ secrets.LIGHTSAIL_USER || 'deploy' }}
          TARGET_DIR: /var/www/production/
        run: |
          if [ -n "$SSH_KEY" ] && [ -n "$SSH_HOST" ]; then
            rsync -avz --delete \
              -e "ssh -i ~/.ssh/id_rsa -o StrictHostKeyChecking=no" \
              frontend/dist/ \
              ${SSH_USER}@${SSH_HOST}:${TARGET_DIR} || \
            rsync -avz --delete \
              -e "ssh -i ~/.ssh/id_rsa -o StrictHostKeyChecking=no" \
              frontend/dist/ \
              ubuntu@${SSH_HOST}:${TARGET_DIR}
            
            # Reload Caddy Server
            ssh -i ~/.ssh/id_rsa -o StrictHostKeyChecking=no ${SSH_USER}@${SSH_HOST} \
              "docker exec sprachcafe_caddy caddy reload --config /etc/caddy/Caddyfile 2>/dev/null || docker exec caddy caddy reload --config /etc/caddy/Caddyfile 2>/dev/null || docker exec library_proxy caddy reload --config /etc/caddy/Caddyfile 2>/dev/null || true" 2>/dev/null || true
            
            echo "✅ Successfully deployed to Production (${TARGET_DIR})"
          else
            echo "⚠️ Deployment skipped: LIGHTSAIL_SSH_KEY secret not present."
          fi
